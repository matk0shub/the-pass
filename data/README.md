# Data

Data files are not tracked by git.

Use:

- `data/raw/` for immutable provider responses or original files.
- `data/normalized/` for derived, schema-controlled research datasets.

Every dataset needs a manifest based on `templates/data_manifest.yaml`.

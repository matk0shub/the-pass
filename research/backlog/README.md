# Research Backlog

Create one file per strategy hypothesis. Each item should point to a `StrategySpec`, source
notes, data requirements, and a kill criterion.

Initial MVP families:

- Time-series momentum.
- Breakout / ORB / Donchian / NR7.
- Funding / carry.

Do not add implementation work until the hypothesis has an explicit `kill_when`.

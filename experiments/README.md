# Experiments

Experiment outputs should be reproducible and receipt-based.

Each run needs:

- `run_receipt.yaml`
- `data_manifest.yaml`
- `metrics_report.yaml`
- `cost_waterfall.yaml`
- `verdict_report.yaml`

Generated run folders belong under `experiments/runs/` and are ignored by git by default.

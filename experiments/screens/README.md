# Screens

Diagnostic screen outputs live here during local work.

Generated screen run files are ignored by git. Public-safe examples should be promoted to
`examples/` only after they have synthetic or explicitly redistributable data.
